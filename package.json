{
  "name": "react-hooks-mock-code-challenge-plantshop",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "json-server": "^0.16.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.10.0",
    "react-scripts": "^5.0.1",
    "router-dom": "^2.2.10"
  },
  "scripts": {
    "start": "react-scripts start",
    "reviews": "json-server --watch db.json -p 6001",
    "airlines": "json-server --watch airlines.json -p 4001",
    "airports": "json-server --watch airports.json -p 8001",
    "test": "jest"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0"
  }
}
